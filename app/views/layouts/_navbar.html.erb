<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-2">
  <div class="container-fluid">
    <%= link_to "Dashboard", root_path, class: "navbar-brand fw-bold text-primary fs-3" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <!-- Home -->
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>

        <!-- Loans Dropdown -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="postsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Loans
          </a>
          <ul class="dropdown-menu" aria-labelledby="postsDropdown">
            <li><%= link_to "All Loans", posts_path, class: "dropdown-item" %></li>
            <li><%= link_to "New Loan", loanselect_post_path, class: "dropdown-item" %></li>

            <!-- ✅ Option 2: Transactions under Loans -->
            <li><%= link_to "Transactions", transactions_path, class: "dropdown-item" %></li>
          </ul>
        </li>

        <!-- ✅ Option 1: Top-level Transactions (use this OR the one above) -->
        <li class="nav-item">
          <%= link_to "Transactions", transactions_path, class: "nav-link" %>
        </li>

        <!-- About -->
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link" %>
        </li>

        <!-- Cards Dropdown -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="supportDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Cards
          </a>
          <ul class="dropdown-menu" aria-labelledby="supportDropdown">
            <li><%= link_to "Credit Card", credit_cards_path, class: "dropdown-item" %></li>
          </ul>
        </li>
      </ul>

      <!-- User Navigation -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarUserDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-person-circle me-1"></i>
              <%= current_user.email %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarUserDropdown">
              <li><%= link_to "Profile", user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "dropdown-item" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary ms-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
